from setuptools import setup

with open('README.md') as f:
    readme = f.read()

setup(
    name='winlock',
    version='0.1.0',
    description='Package to lock Windows directories',
    author='Jake Day'
)
